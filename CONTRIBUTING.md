# 投稿

欢迎提 PR。要求只有两条：**中文**，并且**你真的在用它**。

## 提交前

跑一遍检查：

```sh
npx dsh-doctor .
```

它会查 frontmatter 的键名、必需字段、`name` 是否 kebab-case。这些写错会导致技能被 DSH 静默丢弃。

## 技能长什么样

```
skills/<name>/SKILL.md
```

```markdown
---
name: your-skill          # 必需，kebab-case，与目录名一致
description: 当……时使用，然后说清它会做什么。
---

# 标题

（正文是直接给模型的指令）
```

## 写法标准

参照 DSH 官方自己的 [`.agents/skills/`](https://github.com/deepseek-ai/deepseek-harness/tree/master/.agents/skills)，11 个技能，是最好的样本。

从中总结出的几条，本仓库照此执行：

**1. 是判断指引，不是清单。** 官方技能开头常有一句 "This skill is guidance, not a complete checklist"。清单会让模型机械打勾，判断标准让它权衡。

**2. 列出事实来源。** 如果技能依赖外部文档，指出它在哪，并说明「读它，不要复述它」。这样技能不会变成会过期的二手资料。

**3. 分层。** 阻断项 / 检查项 / 不要做的事，优先级要显式。

**4. 给正例反例对照。** 比抽象描述有效得多。

**5. 单独写「不要做的事」。** 模型有默认倾向，不明说就会做。这一节挡住的问题往往比「要做什么」更多。

## description 决定它会不会被用到

模型最初只看到一份目录，里面只有 `name` 和 `description`，正文要等它决定加载之后才读。

所以 `description` 的唯一任务是让模型在正确时机想起它：

```yaml
# 无效
description: 一个用于代码审查的技能

# 有效
description: 当需要审查代码改动、pull request 或 diff 时使用，按正确性、
  生命周期、安全、测试强度的顺序给出中文审查意见。
```

以「当……时使用」开头。这是官方 11 个技能的统一格式。

## 自测

PR 里请说明你怎么验证的。至少：

1. **它出现在技能列表里** —— 没有就是 frontmatter 问题
2. **description 能触发它** —— 用真实场景描述问一句，看模型会不会加载
3. **正文真的改变了行为** —— 对比加载前后的输出差异

第三条最容易跳过，也最重要。没有差异说明写的是废话。

## 不收什么

- 只是把某个文档复述一遍的技能
- 没有实际使用过的技能
- 与现有技能高度重叠的（可以改进现有的）
- 英文技能（官方内置的已经是英文，本仓库补的是中文空缺）

## 许可

提交即表示同意以 MIT 协议发布。
